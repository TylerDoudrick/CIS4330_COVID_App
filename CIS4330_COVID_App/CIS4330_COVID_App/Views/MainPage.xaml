<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CIS4330_COVID_App.Views.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding lblCurrentLocation}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding lblHomeLocation}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding lblDistance}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Button Text="Click to Update Home!"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="Center"
                Clicked="btnUpdate_Click" />
            <Button Text="Change to Fake Location"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="Center"
                Clicked="btnChangeLocation_Click" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>